package jade.interfaces;

public interface IScene {

    /**
     * Questo metodo si occupa di aggiornare i dati
     * @param dt tempo di differenza tra due frame (delta)
     */
    void update(final float dt);
}
